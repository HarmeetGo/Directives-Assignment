import { HoverrDirective } from './hoverr.directive';

describe('HoverrDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverrDirective();
    expect(directive).toBeTruthy();
  });
});
